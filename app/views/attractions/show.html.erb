<%= @attraction.name %>
<%= link_to "Edit Attraction", edit_attraction_path(@attraction) if current_user.admin %>
<%= button_to "Go on this ride", rides_path, params: {attraction_id: @attraction.id} %>